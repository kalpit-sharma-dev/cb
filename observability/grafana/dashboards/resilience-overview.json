{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "type": "timeseries",
      "title": "CB Failure Rate",
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "targets": [
        {
          "expr": "resilience_demo_cb_failure_rate",
          "legendFormat": "{{name}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "CB Slow Call Rate",
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "targets": [
        {
          "expr": "resilience_demo_cb_slow_call_rate",
          "legendFormat": "{{name}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Retry Events",
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 8},
      "targets": [
        {
          "expr": "rate(resilience_demo_retry_events_total[1m])",
          "legendFormat": "{{name}} {{event}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Bulkhead Events",
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 8},
      "targets": [
        {
          "expr": "rate(resilience_demo_bulkhead_events_total[1m])",
          "legendFormat": "{{name}} {{outcome}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Rate Limiter Events",
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 8},
      "targets": [
        {
          "expr": "rate(resilience_demo_rate_limiter_events_total[1m])",
          "legendFormat": "{{name}} {{outcome}}"
        }
      ]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["resilience", "otel"],
  "templating": {"list": []},
  "time": {"from": "now-15m", "to": "now"},
  "timepicker": {},
  "timezone": "",
  "title": "Resilience Overview",
  "version": 1,
  "weekStart": ""
}
